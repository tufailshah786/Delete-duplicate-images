# Delete-the-duplicate-images-in-folder
Delete the duplicate images in the given folder along with that a "deleted_images.txt" file is created in the given folder that contain the list of deleted images.
